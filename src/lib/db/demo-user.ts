// TODO(auth): scope item/collection queries by the signed-in user. Until then,
// the dashboard's data still belongs to the seeded demo user (the sidebar
// footer already reflects the real signed-in user via the session).
export const DEMO_USER_EMAIL = "demo@devstash.io";
